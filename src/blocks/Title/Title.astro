---
const { text, level = 2 } = Astro.props;

const Tag = `h${level}`;
---

<div class="layout-text">
  <Tag class:list={["title-block"]}>{text}</Tag>
</div>

<style>
  .title-block {
    padding-block: var(--spacing-xl) var(--spacing-lg);
  }
</style>
