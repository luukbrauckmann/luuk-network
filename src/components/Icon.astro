---
const { name } = Astro.props;
---

<svg aria-hidden data-icon={name}>
  <use xlink:href={`#${name}`}></use>
</svg>

<style>
  svg {
    display: inline-block;
    width: 1rem;
    height: 1rem;
    color: currentColor;
  }
</style>
