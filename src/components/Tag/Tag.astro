---
interface Props {
  attributes: any;
  content: string;
  tag: string;
}

const { attributes, content, tag } = Astro.props;

const HTMLTag = tag;
---

<HTMLTag {...attributes}>{content}</HTMLTag>
