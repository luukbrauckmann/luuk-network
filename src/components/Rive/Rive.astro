---
interface Props {
  src: string,
  fit?: "Contain" | "Cover",
  alignment?: "center" | "top" | "bottom" | "left" | "right" | "top-left" | "top-right" | "bottom-left" | "bottom-right",
  autoplay?: boolean,
  stateMachines?: string | string[]
}

const {
  src,
  fit = "Contain",
  alignment = "center",
  autoplay = true,
  stateMachines = ""
} = Astro.props
---

<rive-canvas>
  <canvas width="200" height="200" data-src={src} data-fit={fit} data-alignment={alignment} data-autoplay={autoplay} data-statemachines={stateMachines}>
    <slot />
  </canvas>
</rive-canvas>

<script src="./Rive.client.js"></script>