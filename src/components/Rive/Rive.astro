---
interface Props {
  src: string,
  name: string,
  fit?: "Contain" | "Cover",
  alignment?: "center" | "top" | "bottom" | "left" | "right" | "top-left" | "top-right" | "bottom-left" | "bottom-right",
  autoplay?: boolean,
  stateMachines?: string | string[]
}

const {
  src,
  name,
  fit = "Contain",
  alignment = "center",
  autoplay = true,
  stateMachines = ""
} = Astro.props;
---

<rive-canvas>
  <canvas width="320" height="320" data-src={src} data-fit={fit} data-alignment={alignment} data-autoplay={autoplay} data-statemachines={stateMachines}></canvas>
</rive-canvas>

<script src="./Rive.client.js"></script>